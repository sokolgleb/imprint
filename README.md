# Imprint 🎨

**Imprint** — это высокопроизводительная Python-библиотека для процедурной генерации уникальных графических отпечатков. Система переводит цифровой след (хеш) в визуальный код, используя многослойное наложение геометрических примитивов.

---

## 🛠 Технический стек
* **Язык:** Python 3.12+
* **Графическое ядро:** Pillow (PIL)
* **Валидация данных:** Pydantic (BaseModel)
* **Менеджер пакетов:** [uv](https://github.com)

---

## 📐 Глубокое описание архитектуры

### 1. Модель управления данными `DrawSettings`
Класс наследуется от `BaseModel` и отвечает за подготовку математического контекста отрисовки.

*   **`hash` (str)**: Исходный ключ генерации.
*   **`canvas_size` (int)**: Размер стороны квадратного холста.
*   **`scale_factor` (property)**: Вычисляется как `canvas_size / 1000`. Позволяет сохранять визуальную плотность узора. Если масштаб 2000px, линии станут в 2 раза толще автоматически.
*   **`bytes_list` (property)**: Преобразует хеш в список `int` (0-255). Используется для извлечения параметров цвета и углов.

### 2. Базовая логика `DrawerBase`
Абстрактный класс, определяющий интерфейс для всех слоев отрисовки.
*   **`get_base_color_from_hash`**: Берет первый байт хеша и переводит его в цветовой тон (Hue) от 0 до 360°.
*   **`get_rgb_color`**: Статический хелпер для конвертации HSV в RGB, обеспечивающий сочные и насыщенные цвета.
*   **`draw`**: Абстрактный метод, который должен вернуть модифицированный объект `ImageDraw`.

---

## 🧬 Алгоритмы слоев (Drawers)

### 💠 CrystalDrawer (Слой кристаллов)
Создает радиальную структуру, имитирующую рост кристаллов или лучи света.
*   **Секторная логика**: Весь круг (360°) делится на сегменты пропорционально `percentage` из `chars_stats`.
*   **Плотность**: Количество лучей рассчитывается через логарифм: `60 * (1 + 0.3 * log10(symbols_count))`. Это дает визуальную сложность на больших объемах данных.
*   **Случайность**: Каждый луч имеет случайную длину (70-110% от радиуса) и случайный угол внутри своего сектора.

### 🌀 KaleidoscopeDrawer (Слой калейдоскопа)
Генерирует сложные фрактальные узоры.
*   **Симметрия**: Количество секторов определяется динамически: `4 + (byte % 9)`.
*   **Random Walk**: Алгоритм генерирует путь «пьяной точки» (движение в случайном направлении на шаг `step_dist`), который затем зеркально копируется во все сектора через матрицу поворота.
*   **Ограничение**: Если «точка» выходит за пределы 45% радиуса холста, она возвращается назад, создавая плотный центр.

### 🔴 CoreDrawer (Слой ядра)
Создает фокальную точку композиции.
*   **Адаптивный радиус**: Рассчитывается как `25 * scale_factor * (1 + log10(symbols_count))`.
*   **Цвет**: Если цвет не передан в конструктор, он генерируется на основе базового тона с пониженной яркостью (Value=60) для создания эффекта глубины.

---

## ⚙️ GraphicEngine (Оркестратор)
Движок управляет процессом сборки финального изображения:
1. Создает основной холст `main_img` (белый фон).
2. Создает прозрачный слой `overlay` (RGBA).
3. Поочередно пропускает объект `canvas` через список всех `drawers`.
4. Выполняет `Image.alpha_composite`, чтобы корректно смешать слои с учетом их прозрачности (Alpha).

---

## 🚀 Установка и разработка

### Быстрый старт с `uv`
```bash
# Инициализация окружения
uv sync

# Добавление зависимостей
uv add pillow pydantic
```